import React from 'react';

export default function FrontendPage() {
  return (
    <div>
      <h1>Frontend Page</h1>
      <p>This is a frontend page using the Pages Router.</p>
      <p>The problem is if this file name is the same as the one in src/app then that one will take precedence.</p>
    </div>
  );
};