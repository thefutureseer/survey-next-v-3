'use client';
import React from 'react';

export default function FrontendPage() {
  return (
    <div>
      <h1>Frontend Page</h1>
      <p>This is a frontend page using the App Router.</p>
      <p>Use src/app for new projects or when you 
        need advanced routing and server-side 
        rendering features provided by the 
        App Router.
      </p>
    </div>
  );
};